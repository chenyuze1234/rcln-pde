"""
RCLN-PDE models package (paper companion subset).

Modules are imported directly, e.g. ``from models.rcln_upi_v5 import RCLN_UPI_v5``;
this __init__ is intentionally import-free so that submodules can be used
independently.
"""
